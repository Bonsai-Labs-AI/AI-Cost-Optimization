node_modules/
dist/
.astro/
.DS_Store
*.log
.env
.env.*
# StatiCrypt writes this when run without --config false; the encrypt step
# passes an explicit salt so it's never needed. Never commit it.
.staticrypt.json
